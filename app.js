(function () {
	var app = angular.module("splashCard", []);
	app.controller('flashcardController', function () {
		this.card = content;
	});
	var content = {
		title: 'Pre-Algebra',
		question: 'What is the Commutative Property?',
		answer: 'a + b = b + a'
	};


		

})();
